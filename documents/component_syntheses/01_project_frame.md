# Project Frame

The project now prioritizes a compact refinement loop: prepare data, train the predictive encoder, run trained-encoder discovery refinements, and validate selected discoveries.

The live scientific question is whether geometry, objective, normalization, and pooling refinements make the main encoder idea work better. Broad comparison suites are intentionally out of the default path.
