# Evaluation And Refinement

`pcc-evaluate` remains a lightweight predictive sanity check.

`pcc-refine` is the main discovery comparison surface. It runs trained-encoder arms only: raw, post-hoc token-normalized, probe-weighted, and oracle-aligned diagnostic geometry.

Oracle-aligned rows are diagnostic and must remain marked as not claim-safe.
